a = ['Mary', 'had', 'a', 'little', 'lamb']
for i,j in enumerate(a):
    print(i,j)
# in order to keep the count of the elements while we iterate through any iterable we need to
# use the enumerate function